# 检索、证据与写作方法

## 综述类型与范围

本项目为问题驱动的叙事综述，最近检索/核验日期 2026-09-16，以 LLM 为主，覆盖深度学习学习曲线起源至当日可核验的代表工作。它不宣称完成 PRISMA 式穷尽筛选；不虚构初筛数量、排除数量、引用计数或实验复现。

参考仓库的方法拆解见 [说明](docs/00-reference-method.md)。参考其结构，不把参考综述中的技术判断直接当作证据。

## 两条并行的发现渠道

1. 学术渠道：从 Hestness、Kaplan、Chinchilla、数据受限扩展、test-time compute 等种子出发，查原论文的参考文献、直接回应与复现；用 arXiv、会议/期刊网页和作者/机构报告核验。
2. 社区渠道：检索 X、YouTube、Reddit 的讨论。保存原帖/视频链接、观测日期、可见指标、检索方式和对应主源。社区数据只确定关注与争议，不确定论文真伪或结论可靠性。

本次实际使用可用的网页检索工具与公开论文 HTML。技能建议的 Parallel CLI 与 API 凭据在当前环境不可用，未将其写成已执行的检索。参考 GitHub 通过公开归档读取；原始全文只在临时阅读目录，不纳入发布。

## 纳入标准

满足至少一个条件：建立可检查的规模—性能关系；改变预算最优配置；暴露关键假设或外推失败；提供可靠复现/反证；给出足以改变研究实践的数据、架构、后训练、推理或评测框架。单次大模型发布若没有方法和证据细节，通常只作上下文。

论文与报告必须存在可核验的主源；只读摘要的工作标为浅阅读，重大主张不进入定论。观点 blog 可以解释研究问题，不用于替代实验论证。社区热帖无可读主源时列为待查，不能提升为技术结论。

## 提取单位与写作模板

每个关键节点回答：原来什么做不到；核心洞见是什么；方法改了哪个变量或机制；证据来自什么模型/数据/预算/指标；解决了哪项限制；哪些限制仍在；与下一节点是什么关系。

“直接承接”要求论文明确回应前作；“并行路线”表示共同问题；“本综述归纳”表示解释性联系。图示默认最后一种，不把相关性写成必然历史因果。

节点深度分层：转折点展开机制与假设，重要跟进写清修复与代价，边缘材料保留索引。章节结尾先整理分支，再提出有具体测量对象的开放问题。

## 证据检查

- 分清交叉熵、归一化错误率、pass@1、pass@k、选择器准确率，避免共享一个“性能”标签。
- 记录模型与数据定义、活动/总参数、tokenizer、优化器和学习率计划；跨论文数字不能默认可比。
- 区分 FLOPs、GPU 小时、延迟、吞吐、费用和能耗；漏算的生成、验证、筛选成本明确标注。
- 区分单次观察、多个随机种子、受控消融、跨域测试、外部复现和理论证明。
- 关注修订版本，不把 preprint、正式发表与独立复现混为一谈；发表也不自动等于结论稳固。
- 记录不可访问来源，不把搜索摘要写成全文阅读；技术断言附主源链接。

## 热度口径

首版仅使用平台内可见信号筛选值得关注的材料，不做跨平台排行榜。YouTube 观看量与 Reddit score 的阈值是编辑工作阈值，详见社区章；X 未取得可核验指标时为 `null`。搜索索引快照的真实计数时刻未知，观测日期不等于计数生成日期。

## 可追溯记录

`sources/*search-log*` 保存查询、访问/筛选限制；`sources/foundations.json`、`frontier.json`、`extensions.json`、`expansion-*.json` 和 `editorial.json` 保存论文、报告与作者技术文章；`community.json` 单独保存讨论材料。每条至少有 ID、标题、年份、URL、分支、问题、洞见、局限、阅读深度、核验日期。若作者/版本/日期未知，应保留缺失，不猜测。

首版完成了引用与关键主张检查，未重新训练模型、运行原始基准或逐式复算所有附录。首版审查见 [审查记录](sources/peer-review-notes.md)；v0.2 的定点交叉审阅见 [后训练与推理](sources/v0.2-review-foundations.md) 和 [预训练、数据与架构](sources/v0.2-review-frontier.md)。

## 正文覆盖审计

构建时对七篇技术章的主源链接做规范化和去重，生成 `data/citation-audit.json` 及正文覆盖地图。文献库条数、正文引用条数与完整阅读条数分别理解：目录有记录不意味着已经精读，正文有引用也不等于已重跑实验。优先补齐能够解释研究分叉的关键证据，而不是为了数量将无关材料计入。
